#lang racket

; He's pulling the first item of the input list
; and building an output list with that item as
; the last.

; The first argument in cons becomes the first item,
; so if it's a list, that list is the first item.